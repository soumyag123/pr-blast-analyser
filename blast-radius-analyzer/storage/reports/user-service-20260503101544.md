# Blast Radius Report

## Summary

Detected 2 breaking contract change(s) in user-service with 1 downstream impact finding(s).

## Contract Changes

- `GET /users/{id}`
  - Change type: `field_removed`
  - Field: `user_id`
  - Breaking: `true`
- `GET /users/{id}`
  - Change type: `field_type_changed`
  - Field: `preferences`
  - Breaking: `true`

## Impact Findings

- Service: `notification-service`
  - Severity: `high`
  - Confidence: `high`
  - Summary: notification-service appears to depend on GET /users/{id} and references user_id, so this field_removed may break it.
  - Evidence: `services/notification-service/clients.py:1-4`
    - Snippet: `def fetch_user(user_id: str) -> dict:     """Fetch a user payload from user-service."""     endpoint = "/users/{id}"     response = {`
  - Evidence: `services/notification-service/clients.py:1-6`
    - Snippet: `def fetch_user(user_id: str) -> dict:     """Fetch a user payload from user-service."""     endpoint = "/users/{id}"     response = {         "id": user_id,         "email": "user@example.com",`
  - Evidence: `services/notification-service/clients.py:2-8`
    - Snippet: `"""Fetch a user payload from user-service."""     endpoint = "/users/{id}"     response = {         "id": user_id,         "email": "user@example.com",         "user_id": user_id,         "preferences": {"channel": "email"}`
  - Evidence: `services/notification-service/processor.py:1-4`
    - Snippet: `def build_notification_payload(user_id: str) -> dict:     """Build a notification payload from a user response."""     user = fetch_user(user_id)     endpoint = "/users/{id}"`
  - Evidence: `services/notification-service/processor.py:1-6`
    - Snippet: `def build_notification_payload(user_id: str) -> dict:     """Build a notification payload from a user response."""     user = fetch_user(user_id)     endpoint = "/users/{id}"     payload = user["payload"]     return {`
  - Evidence: `services/notification-service/processor.py:1-7`
    - Snippet: `def build_notification_payload(user_id: str) -> dict:     """Build a notification payload from a user response."""     user = fetch_user(user_id)     endpoint = "/users/{id}"     payload = user["payload"]     return {         "endpoint": endpoint,`
  - Evidence: `services/notification-service/README.md:1-3`
    - Snippet: `# Notification Service  This service calls GET /users/{id} on user-service and uses user_id to compose notification payloads.`