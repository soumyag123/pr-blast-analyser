# Blast Radius Report

## Summary

Detected 2 breaking contract change(s) in user-service with 5 downstream impact finding(s).

## Contract Changes

- `GET /users/{id}`
  - Change type: `field_removed`
  - Field: `user_id`
  - Breaking: `true`
- `GET /users/{id}`
  - Change type: `field_type_changed`
  - Field: `preferences`
  - Breaking: `true`

## Impact Findings

- Service: `notification-service`
  - Severity: `high`
  - Confidence: `high`
  - Summary: notification-service appears to depend on GET /users/{id} and references user_id, so this field_removed may break it.
  - Evidence: `services/notification-service/clients.py:2-8`
    - Snippet: `"""Fetch a user payload from user-service."""     endpoint = "/users/{id}"     response = {         "id": user_id,         "email": "user@example.com",         "user_id": user_id,         "preferences": {"channel": "email"}`
  - Evidence: `services/notification-service/processor.py:3-9`
    - Snippet: `user = fetch_user(user_id)     endpoint = "/users/{id}"     payload = user["payload"]     return {         "endpoint": endpoint,         "recipient_user_id": payload["user_id"]     }`
- Service: `billing-service`
  - Severity: `low`
  - Confidence: `low`
  - Summary: billing-service may depend on GET /users/{id}, but only weak evidence was found for user_id.
- Service: `notification-service`
  - Severity: `medium`
  - Confidence: `high`
  - Summary: notification-service appears to depend on GET /users/{id} and references preferences, so this field_type_changed may break it.
  - Evidence: `services/notification-service/clients.py:2-9`
    - Snippet: `"""Fetch a user payload from user-service."""     endpoint = "/users/{id}"     response = {         "id": user_id,         "email": "user@example.com",         "user_id": user_id,         "preferences": {"channel": "email"}     }`
- Service: `billing-service`
  - Severity: `low`
  - Confidence: `low`
  - Summary: billing-service may depend on GET /users/{id}, but only weak evidence was found for preferences.
- Service: `analytics-service`
  - Severity: `low`
  - Confidence: `low`
  - Summary: analytics-service may depend on GET /users/{id}, but only weak evidence was found for preferences.